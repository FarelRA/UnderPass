{
  "name": "conduit",
  "version": "1.0.1",
  "description": "Edge network permissive CORS proxy implemented in Cloudflare Workers",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": ">=4.0.0"
  }
}
